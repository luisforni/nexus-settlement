apiVersion: v2
name: nexus-settlement
description: >
  Nexus Settlement Platform — multi-service Helm chart bundling the API gateway,
  settlement processor, fraud detection engine, and notification service.
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - fintech
  - settlement
  - fraud-detection
  - kafka
  - fastapi
  - typescript

home: https://github.com/your-org/nexus-settlement
sources:
  - https://github.com/your-org/nexus-settlement

maintainers:
  - name: Platform Engineering
    email: platform@example.com

annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Initial chart release
